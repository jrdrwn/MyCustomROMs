<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCustomROMs" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyCustomROMs.LaunchTheme" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="Theme.MyCustomROMs" parent="Base.Theme.MyCustomROMs" />
</resources>